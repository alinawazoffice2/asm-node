def proc main
    mov dx name=test
	pts dx
	mov dx table=abc
	pts dx
	mov dx data=123
	pts dx
	stf test.ad    
    pfs dx
    pfs dx
    pfs dx
    mov dx data=456
    pts dx
    stfp test.ad
end proc